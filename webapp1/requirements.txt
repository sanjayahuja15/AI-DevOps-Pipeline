# Project Requirements:
- The system must log errors automatically.
- A user should be able to log in and access a dashboard.
- The data analytics feature must be updated every 24 hours.
- Notifications should be sent when an error is detected.
- The project deadline is 2 weeks.

# Dependencies:
flask==2.3.2
matplotlib==3.10.0
psutil==5.9.0
